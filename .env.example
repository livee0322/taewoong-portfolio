# Local/admin-foundation preview only. Production must also enforce an authenticated admin session.
ADMIN_FOUNDATION_ENABLED=false

# Future Supabase adapter. Do not commit real values.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
